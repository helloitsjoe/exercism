#![allow(unused_imports)]
mod exercises;

use exercises::allergies;

fn main() {
    // let a = allergies::Allergies.new();
    // reverse_string::reverse("foo");
    // gigasecond::after(Utc::now());
}
